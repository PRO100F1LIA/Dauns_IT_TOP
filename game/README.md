# Breaking Bonds: A Chemistry Chronicle

Образовательная игра-новелла по химии, вдохновленная сериалом "Во все тяжкие". Игра сочетает в себе сюжет, диалоги с персонажами и решение химических головоломок для прогресса.

## 📋 Описание

**Breaking Bonds** — это интерактивная обучающая игра, где игрок выступает в роли нового стажера, нанятого командой Хайзенберга. Чтобы доказать свою ценность, необходимо решать химические задачи, начиная с основ и доходя до сложного синтеза.

### Особенности:
- 🎮 Графический интерфейс в темной теме (стиль Breaking Bad)
- 👥 Персонажи из сериала: Уолтер Уайт, Джесси Пинкман, Гейл Беттикер, Густаво Фринг
- 🧪 5 уровней сложности с различными типами задач
- 📚 Обучение основам химии через интерактивные задания
- 🎯 Задачи: молярная масса, конвертация моли↔граммы, балансировка уравнений, стехиометрия

## 🔧 Требования

### Обязательные компоненты:
- **Windows 10/11** (или Linux/macOS)
- **Visual Studio 2019/2022** (или другой компилятор C++17)
- **CMake 3.16** или выше
- **Qt 5.15+** или **Qt 6.x** (Core и Widgets)
- **Git** (для клонирования репозитория, опционально)

## 📦 Установка компонентов

### 1. Установка Visual Studio

1. Скачайте **Visual Studio Community** (бесплатная версия):
   - Сайт: https://visualstudio.microsoft.com/
   
2. При установке выберите рабочую нагрузку:
   - ✅ **Desktop development with C++**
   - ✅ **CMake tools for Windows** (включается автоматически)

3. В дополнительных компонентах убедитесь, что установлено:
   - ✅ Windows 10/11 SDK
   - ✅ MSVC компилятор (x64/x86)

### 2. Установка CMake

#### Вариант A: Через установщик (рекомендуется)

1. Скачайте CMake с официального сайта:
   - Сайт: https://cmake.org/download/
   - Выберите: **Windows x64 Installer**

2. Запустите установщик и следуйте инструкциям

3. При установке выберите опцию:
   - ✅ **Add CMake to system PATH**

4. Проверьте установку:
   ```powershell
   cmake --version
   ```
   Должна отобразиться версия 3.16 или выше.

#### Вариант B: Через Visual Studio

CMake может быть установлен вместе с Visual Studio через рабочую нагрузку "Desktop development with C++".

### 3. Установка Qt

1. Скачайте Qt через **Qt Online Installer**:
   - Сайт: https://www.qt.io/download-open-source
   - Зарегистрируйтесь (бесплатно) или используйте Qt Account

2. Запустите установщик и выберите:
   - ✅ **Qt 6.5.x** или **Qt 5.15.x** (рекомендуется Qt 6)
   - ✅ Компилятор: **MSVC 2019 64-bit** или **MSVC 2022 64-bit** (в зависимости от версии Visual Studio)
   - ✅ Компоненты:
     - ✅ Qt Core
     - ✅ Qt Widgets
     - ✅ CMake integration (опционально, но полезно)

3. Запомните путь установки (по умолчанию):
   - Qt 6: `C:\Qt\6.x.x\msvc2019_64` или `C:\Qt\6.x.x\msvc2022_64`
   - Qt 5: `C:\Qt\5.15.x\msvc2019_64`

4. **Важно**: Добавьте Qt в PATH или запомните путь для дальнейшей настройки

## 🚀 Сборка и запуск проекта

### Способ 1: Через Visual Studio (рекомендуется)

#### Шаг 1: Открытие проекта

1. Запустите **Visual Studio**

2. Выберите:
   - **File → Open → Folder** (или **Файл → Открыть → Папка**)

3. Выберите папку проекта:
   ```
   C:\Users\Tim\source\repos\Dauns_IT_TOP\game
   ```

4. Visual Studio автоматически обнаружит `CMakeLists.txt` и начнет настройку проекта

#### Шаг 2: Настройка CMake (если Qt не найден)

Если Visual Studio не может найти Qt, выполните одно из действий:

**Вариант A: Настройка через CMakeSettings.json**

1. В Visual Studio: **Project → CMake Settings for BreakingBonds**

2. Добавьте переменную окружения:
   - Name: `CMAKE_PREFIX_PATH`
   - Value: `C:\Qt\6.x.x\msvc2019_64` (укажите ваш путь к Qt)

**Вариант B: Настройка через CMakeLists.txt**

Добавьте в начало `CMakeLists.txt` (после `cmake_minimum_required`):

```cmake
# Укажите путь к Qt (замените на ваш путь)
set(CMAKE_PREFIX_PATH "C:/Qt/6.5.0/msvc2019_64" ${CMAKE_PREFIX_PATH})
```

#### Шаг 3: Выбор конфигурации

1. В панели инструментов Visual Studio выберите:
   - **Конфигурация**: Debug или Release
   - **Платформа**: x64

2. Подождите, пока Visual Studio сгенерирует проект (первый раз может занять несколько минут)

#### Шаг 4: Сборка и запуск

1. **Сборка проекта**:
   - Нажмите `Ctrl+Shift+B` (Build → Build All)
   - Или: **Build → Build BreakingBonds**

2. **Запуск игры**:
   - Нажмите `F5` (Start Debugging)
   - Или: `Ctrl+F5` (Start Without Debugging)

3. Игра запустится в новом окне!

### Способ 2: Через командную строку (CMake + Visual Studio)

#### Шаг 1: Открытие командной строки разработчика

1. Откройте **"Developer Command Prompt for VS"**:
   - В меню Пуск: **Visual Studio → Developer Command Prompt for VS**

2. Перейдите в папку проекта:
   ```cmd
   cd C:\Users\Tim\source\repos\Dauns_IT_TOP\game
   ```

#### Шаг 2: Генерация проекта

1. Создайте папку для сборки:
   ```cmd
   mkdir build
   cd build
   ```

2. Сгенерируйте проект Visual Studio:

   **Для Visual Studio 2022:**
   ```cmd
   cmake .. -G "Visual Studio 17 2022" -A x64 -DCMAKE_PREFIX_PATH="C:\Qt\6.x.x\msvc2022_64"
   ```

   **Для Visual Studio 2019:**
   ```cmd
   cmake .. -G "Visual Studio 16 2019" -A x64 -DCMAKE_PREFIX_PATH="C:\Qt\6.x.x\msvc2019_64"
   ```

   **Важно**: Замените `C:\Qt\6.x.x\msvc2022_64` на ваш реальный путь к Qt!

3. Если все прошло успешно, будет создан файл `BreakingBonds.sln`

#### Шаг 3: Сборка

1. Соберите проект:
   ```cmd
   cmake --build . --config Release
   ```
   Или для Debug:
   ```cmd
   cmake --build . --config Debug
   ```

2. Исполняемый файл будет создан в:
   ```
   build\Release\BreakingBonds.exe
   ```
   или
   ```
   build\Debug\BreakingBonds.exe
   ```

#### Шаг 4: Запуск

1. Запустите исполняемый файл:
   ```cmd
   Release\BreakingBonds.exe
   ```

### Способ 3: Через командную строку (CMake + компилятор напрямую)

```cmd
cd C:\Users\Tim\source\repos\Dauns_IT_TOP\game
mkdir build
cd build
cmake .. -G "NMake Makefiles" -DCMAKE_PREFIX_PATH="C:\Qt\6.x.x\msvc2019_64"
nmake
```

## 🐛 Решение проблем

### Проблема: CMake не находит Qt

**Симптомы**: Ошибка `Could NOT find Qt` или `Could not find a package configuration file`

**Решения**:

1. **Проверьте путь к Qt**:
   ```powershell
   dir C:\Qt
   ```
   Убедитесь, что папка существует.

2. **Укажите путь явно в CMakeLists.txt**:
   ```cmake
   set(CMAKE_PREFIX_PATH "C:/Qt/6.5.0/msvc2019_64" ${CMAKE_PREFIX_PATH})
   ```

3. **Используйте переменную окружения**:
   ```powershell
   $env:CMAKE_PREFIX_PATH="C:\Qt\6.5.0\msvc2019_64"
   cmake ..
   ```

### Проблема: Ошибки компиляции Qt (moc, uic, rcc)

**Симптомы**: Ошибки типа `moc_*.cpp` не найден

**Решения**:

1. Убедитесь, что в `CMakeLists.txt` включены:
   ```cmake
   set(CMAKE_AUTOMOC ON)
   set(CMAKE_AUTORCC ON)
   set(CMAKE_AUTOUIC ON)
   ```
   (Уже включены в текущей версии)

2. Пересоберите проект:
   ```cmd
   cmake --build . --clean-first
   ```

### Проблема: Несовпадение версий компилятора

**Симптомы**: Ошибки линковки или несовместимость библиотек

**Решения**:

1. Убедитесь, что версия Qt соответствует компилятору:
   - Visual Studio 2019 → Qt с MSVC 2019
   - Visual Studio 2022 → Qt с MSVC 2022

2. Проверьте архитектуру:
   - Qt x64 → проект x64
   - Qt x86 → проект x86 (не рекомендуется)

### Проблема: Игра не запускается (ошибка DLL)

**Симптомы**: Ошибка "Qt5Core.dll не найден" или подобная

**Решения**:

1. Добавьте путь к Qt DLL в PATH:
   ```powershell
   $env:PATH="C:\Qt\6.x.x\msvc2019_64\bin;$env:PATH"
   ```

2. Или скопируйте необходимые DLL в папку с exe файлом:
   - `Qt6Core.dll`
   - `Qt6Gui.dll`
   - `Qt6Widgets.dll`
   - И соответствующие плагины

3. Или используйте `windeployqt` (встроенная утилита Qt):
   ```cmd
   cd build\Release
   C:\Qt\6.x.x\msvc2019_64\bin\windeployqt.exe BreakingBonds.exe
   ```

### Проблема: Высокое потребление ресурсов при сборке

**Решения**:

1. Используйте Release конфигурацию для финальной сборки
2. Отключите отладку Qt (если не нужна)

## 📁 Структура проекта

```
game/
├── CMakeLists.txt          # Файл конфигурации CMake
├── README.md               # Этот файл
└── src/
    ├── main.cpp            # Точка входа
    ├── GameEngine.h/cpp    # Управление состоянием игры
    ├── ChemistryEngine.h/cpp # Ядро химических расчетов
    ├── DialogSystem.h/cpp  # Система диалогов и задач
    └── UI_MainWindow.h/cpp # Qt GUI окно
```

## 🎮 Игровой процесс

1. **Начало игры**: Нажмите "НАЧАТЬ ИГРУ"
2. **Диалог**: Прочитайте диалог персонажа
3. **Задача**: Решите химическую задачу
4. **Ответ**: Введите ответ в поле
5. **Результат**: Получите обратную связь от персонажа
6. **Прогресс**: Переходите к следующему уровню

### Уровни:

- **Уровень 1** (Джесси): Расчет молярной массы H₂O
- **Уровень 2** (Уолтер): Конвертация моли → граммы для метамфетамина
- **Уровень 3** (Гейл): Балансировка уравнения горения пропана
- **Уровень 4** (Хайзенберг): Стехиометрия нитрования бензола
- **Уровень 5** (Гус): Расчет чистоты продукта

## 🔨 Разработка

### Добавление новых задач

Задачи определяются в `DialogSystem.cpp` в методе `initializeDefaultTasks()`. 

### Изменение диалогов

Диалоги редактируются в `DialogSystem.cpp` в методе `initializeDefaultDialogs()`.

### Настройка GUI

Стили и внешний вид настраиваются в `UI_MainWindow.cpp` в методе `setupUI()` и `applyDarkTheme()`.

## 📝 Лицензия

Этот проект создан в образовательных целях.

## 🤝 Вклад

Проект является обучающим примером. Для улучшения кода или добавления функций создайте issue или pull request.

## 📧 Контакты

При возникновении проблем с установкой или запуском:
1. Проверьте раздел "Решение проблем"
2. Убедитесь, что все компоненты установлены корректно
3. Проверьте версии всех установленных программ

---

**Приятной игры и удачного изучения химии! 🧪**

*"Наука, вот в чем суть!"* — Уолтер Уайт


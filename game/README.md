# Breaking Bonds: A Chemistry Chronicle

Образовательная игра-новелла по химии, вдохновленная сериалом "Во все тяжкие". Игра сочетает в себе сюжет, диалоги с персонажами и решение химических головоломок для прогресса.

## 📋 Описание

**Breaking Bonds** — это интерактивная обучающая игра, где игрок выступает в роли нового стажера, нанятого командой Хайзенберга. Чтобы доказать свою ценность, необходимо решать химические задачи, начиная с основ и доходя до сложного синтеза.

### Особенности:
- 🎮 Графический интерфейс в темной теме (стиль Breaking Bad)
- 👥 Персонажи из сериала: Уолтер Уайт, Джесси Пинкман, Гейл Беттикер, Густаво Фринг
- 🧪 5 уровней сложности с различными типами задач
- 📚 Обучение основам химии через интерактивные задания
- 🎯 Задачи: молярная масса, конвертация моли↔граммы, балансировка уравнений, стехиометрия

## 🔧 Требования

### Обязательные компоненты:
- **Windows 10/11**
- **Visual Studio 2019/2022** (Community версия бесплатна)
- **SFML 2.5+** (бесплатная библиотека)

Все компоненты доступны для скачивания в России и полностью бесплатны!

## 📦 Установка компонентов

### 1. Установка Visual Studio Community

1. Скачайте **Visual Studio Community** (полностью бесплатная версия):
   - Сайт: https://visualstudio.microsoft.com/ru/downloads/
   - Выберите "Community" версию

2. При установке выберите рабочую нагрузку:
   - ✅ **Разработка классических приложений на C++** (Desktop development with C++)
   - ✅ Включает MSVC компилятор и Windows SDK автоматически

3. Установка займет около 10-20 минут в зависимости от скорости интернета

### 2. Установка SFML

SFML (Simple and Fast Multimedia Library) — это бесплатная кроссплатформенная библиотека для создания игр и мультимедиа приложений.

#### Способ 1: Прямая загрузка (рекомендуется)

1. Перейдите на официальный сайт SFML:
   - Сайт: https://www.sfml-dev.org/download.php

2. Скачайте версию для **Visual C++**:
   - Для Visual Studio 2019: `SFML-2.5.1-windows-vc16-64-bit.zip`
   - Для Visual Studio 2022: `SFML-2.5.1-windows-vc17-64-bit.zip` или новее
   - ⚠️ Важно: выберите версию, соответствующую вашему компилятору (vc16 = VS2019, vc17 = VS2022)

3. Распакуйте архив в удобное место, например:
   ```
   C:\SFML
   ```
   После распаковки структура должна быть:
   ```
   C:\SFML\
   ├── bin\
   ├── include\
   ├── lib\
   └── ...
   ```

4. **Скопируйте папку SFML в папку проекта:**
   - Скопируйте содержимое папки `SFML-2.5.1-windows-vcXX-64-bit` (папки `bin`, `include`, `lib`) 
   - В папку вашего проекта в новую папку `sfml`:
   ```
   game\
   ├── sfml\
   │   ├── bin\
   │   ├── include\
   │   └── lib\
   ├── src\
   └── ...
   ```

#### Способ 2: Через vcpkg (опционально)

Если у вас установлен vcpkg:
```powershell
vcpkg install sfml:x64-windows
```

Но для простоты рекомендуем Способ 1.

### 3. Проверка установки

Убедитесь, что у вас есть:
- ✅ Visual Studio установлена
- ✅ Папка `sfml` с подпапками `include`, `lib`, `bin` в корне проекта

## 🚀 Сборка и запуск проекта

### Способ 1: Через Visual Studio (рекомендуется)

#### Шаг 1: Открытие проекта

1. Запустите **Visual Studio**

2. Откройте файл решения:
   - **File → Open → Project/Solution** (или **Файл → Открыть → Проект/Решение**)
   - Выберите файл `BreakingBonds.sln` в корне папки проекта

3. Visual Studio откроет проект со всеми файлами

#### Шаг 2: Настройка пути к SFML

Если SFML не найден автоматически, убедитесь что структура папок правильная:

```
game/
├── sfml/           ← Папка SFML должна быть здесь
│   ├── include/
│   ├── lib/
│   └── bin/
├── src/
├── BreakingBonds.sln
└── BreakingBonds.vcxproj
```

Если вы разместили SFML в другом месте, отредактируйте `BreakingBonds.vcxproj`:
- Найдите строки с `$(ProjectDir)sfml\include` и `$(ProjectDir)sfml\lib`
- Замените на ваш путь к SFML

#### Шаг 3: Выбор конфигурации

1. В панели инструментов Visual Studio выберите:
   - **Конфигурация**: Debug или Release (для первого запуска используйте Debug)
   - **Платформа**: x64

2. Если x64 не доступен:
   - **Project → Properties → Configuration Properties → General → Platform Toolset**
   - Убедитесь что выбран правильный компилятор

#### Шаг 4: Сборка проекта

1. **Соберите проект**:
   - Нажмите `Ctrl+Shift+B` (Build → Build Solution)
   - Или: **Build → Build Solution** в меню
   - Или: Правой кнопкой на проект → **Build**

2. Если сборка успешна, вы увидите:
   ```
   ========== Build: 1 succeeded, 0 failed ==========
   ```

#### Шаг 5: Копирование DLL файлов

**Важно**: Перед запуском нужно скопировать DLL файлы SFML в папку с исполняемым файлом.

1. Найдите папку с собранным exe:
   - Debug: `bin\x64\Debug\BreakingBonds.exe`
   - Release: `bin\x64\Release\BreakingBonds.exe`

2. Скопируйте DLL файлы из `sfml\bin` в эту папку:
   - `sfml-graphics-d-2.dll` (Debug) или `sfml-graphics-2.dll` (Release)
   - `sfml-window-d-2.dll` (Debug) или `sfml-window-2.dll` (Release)
   - `sfml-system-d-2.dll` (Debug) или `sfml-system-2.dll` (Release)

3. **Автоматизация**: Создайте скрипт для автоматического копирования (см. ниже)

#### Шаг 6: Запуск игры

1. Нажмите `F5` (Start Debugging) или `Ctrl+F5` (Start Without Debugging)

2. Или запустите exe файл напрямую из папки `bin\x64\Debug\` (или `Release\`)

3. Игра запустится в новом окне!

### Способ 2: Автоматическое копирование DLL (упрощает процесс)

Создайте файл `copy_dll.bat` в корне проекта:

```batch
@echo off
echo Copying SFML DLL files...

if exist "bin\x64\Debug\BreakingBonds.exe" (
    copy /Y "sfml\bin\sfml-graphics-d-2.dll" "bin\x64\Debug\"
    copy /Y "sfml\bin\sfml-window-d-2.dll" "bin\x64\Debug\"
    copy /Y "sfml\bin\sfml-system-d-2.dll" "bin\x64\Debug\"
    echo DLL files copied to Debug folder
)

if exist "bin\x64\Release\BreakingBonds.exe" (
    copy /Y "sfml\bin\sfml-graphics-2.dll" "bin\x64\Release\"
    copy /Y "sfml\bin\sfml-window-2.dll" "bin\x64\Release\"
    copy /Y "sfml\bin\sfml-system-2.dll" "bin\x64\Release\"
    echo DLL files copied to Release folder
)

pause
```

Запускайте этот скрипт после каждой сборки, или добавьте его как Post-Build Event в настройках проекта.

### Способ 3: Настройка Post-Build Event (автоматическое копирование)

1. В Visual Studio: **Project → Properties → Configuration Properties → Build Events → Post-Build Event**

2. В **Command Line** добавьте:
   ```batch
   xcopy /Y "$(ProjectDir)sfml\bin\sfml-*-d-2.dll" "$(OutDir)" 2>nul
   xcopy /Y "$(ProjectDir)sfml\bin\sfml-*-2.dll" "$(OutDir)" 2>nul
   ```

3. Теперь DLL будут копироваться автоматически после каждой сборки!

## 🐛 Решение проблем

### Проблема: Ошибки компиляции "Cannot open include file: 'SFML/...'"

**Причина**: Компилятор не может найти заголовочные файлы SFML

**Решение**:
1. Проверьте, что папка `sfml` находится в корне проекта
2. Убедитесь, что структура папок правильная: `game\sfml\include\SFML\...`
3. В `BreakingBonds.vcxproj` должны быть пути:
   ```xml
   <AdditionalIncludeDirectories>$(ProjectDir)sfml\include;%(AdditionalIncludeDirectories)</AdditionalIncludeDirectories>
   ```

### Проблема: Ошибки линковки "unresolved external symbol"

**Причина**: Компоновщик не может найти библиотеки SFML

**Решение**:
1. Проверьте, что в `BreakingBonds.vcxproj` указан путь к библиотекам:
   ```xml
   <AdditionalLibraryDirectories>$(ProjectDir)sfml\lib;%(AdditionalLibraryDirectories)</AdditionalLibraryDirectories>
   ```
2. Убедитесь, что в `AdditionalDependencies` указаны правильные библиотеки:
   - Debug: `sfml-graphics-d.lib;sfml-window-d.lib;sfml-system-d.lib`
   - Release: `sfml-graphics.lib;sfml-window.lib;sfml-system.lib`

### Проблема: "The program can't start because sfml-xxx-d-2.dll is missing"

**Причина**: DLL файлы SFML не находятся в той же папке, что и exe

**Решение**:
1. Скопируйте DLL из `sfml\bin` в папку с `BreakingBonds.exe`
2. Или добавьте путь к `sfml\bin` в переменную окружения PATH
3. Или используйте Post-Build Event для автоматического копирования (см. выше)

### Проблема: Несовпадение версий компилятора

**Симптомы**: Ошибки при линковке или запуске программы

**Решение**:
1. Убедитесь, что версия SFML соответствует версии компилятора:
   - Visual Studio 2019 → SFML для vc16
   - Visual Studio 2022 → SFML для vc17
2. Скачайте правильную версию SFML с сайта

### Проблема: Шрифт не отображается (текст пустой или квадратики)

**Причина**: Системные шрифты не найдены

**Решение**:
1. Игра пытается загрузить шрифт из `C:\Windows\Fonts\consola.ttf`
2. Если шрифт не найден, текст может отображаться некорректно
3. Проверьте, что папка `C:\Windows\Fonts` существует и доступна
4. Альтернатива: скопируйте файл шрифта (например, `arial.ttf`) в папку проекта и измените путь в `GameWindow.cpp`

### Проблема: Окно не открывается или сразу закрывается

**Причина**: Ошибка выполнения или отсутствующие DLL

**Решение**:
1. Запустите exe из командной строки, чтобы увидеть ошибки
2. Проверьте наличие всех DLL файлов в папке с exe
3. Убедитесь, что используется правильная версия DLL (Debug/Release)

## 📁 Структура проекта

```
game/
├── sfml/                      # SFML библиотека (нужно скачать)
│   ├── bin/                   # DLL файлы
│   ├── include/               # Заголовочные файлы
│   └── lib/                   # Библиотеки для линковки
├── src/
│   ├── main.cpp               # Точка входа
│   ├── GameEngine.h/cpp       # Управление состоянием игры
│   ├── ChemistryEngine.h/cpp  # Ядро химических расчетов
│   ├── DialogSystem.h/cpp     # Система диалогов и задач
│   └── GameWindow.h/cpp       # SFML GUI окно
├── BreakingBonds.sln          # Файл решения Visual Studio
├── BreakingBonds.vcxproj      # Файл проекта Visual Studio
└── README.md                  # Этот файл
```

## 🎮 Игровой процесс

1. **Начало игры**: Нажмите "НАЧАТЬ ИГРУ"
2. **Диалог**: Прочитайте диалог персонажа
3. **Задача**: Решите химическую задачу
4. **Ответ**: Введите ответ в текстовое поле (кликните на него)
5. **Результат**: Получите обратную связь от персонажа
6. **Прогресс**: Переходите к следующему уровню

### Уровни:

- **Уровень 1** (Джесси): Расчет молярной массы H₂O
- **Уровень 2** (Уолтер): Конвертация моли → граммы для метамфетамина
- **Уровень 3** (Гейл): Балансировка уравнения горения пропана (формат: "1 5 3 4")
- **Уровень 4** (Хайзенберг): Стехиометрия нитрования бензола
- **Уровень 5** (Гус): Расчет чистоты продукта

### Управление:

- **Мышь**: Клик по кнопкам, клик по полю ввода для ввода текста
- **Клавиатура**: Ввод ответа, Enter для отправки ответа
- **Esc**: Закрыть игру (можно добавить в будущих версиях)

## 🔨 Разработка

### Добавление новых задач

Задачи определяются в `DialogSystem.cpp` в методе `initializeDefaultTasks()`. 

### Изменение диалогов

Диалоги редактируются в `DialogSystem.cpp` в методе `initializeDefaultDialogs()`.

### Настройка GUI

Стили и внешний вид настраиваются в `GameWindow.cpp`:
- Цвета: константы в начале `GameWindow.cpp`
- Размеры окон: константы `WINDOW_WIDTH`, `WINDOW_HEIGHT` в `GameWindow.h`
- Шрифты: метод `drawText()` в `GameWindow.cpp`

## 📝 Альтернативные способы получения SFML

Если официальный сайт недоступен:

1. **GitHub**: https://github.com/SFML/SFML (исходный код)
2. **SourceForge**: https://sourceforge.net/projects/sfml/ (зеркала)
3. **Локальные зеркала**: Многие университеты и организации имеют зеркала

## 📧 Поддержка

При возникновении проблем:

1. Проверьте раздел "Решение проблем" выше
2. Убедитесь, что все компоненты установлены корректно
3. Проверьте версии всех установленных программ
4. Убедитесь, что структура папок соответствует описанию

## 🎯 Быстрый старт (краткая инструкция)

1. ✅ Установите Visual Studio Community
2. ✅ Скачайте SFML и распакуйте в папку `game\sfml\`
3. ✅ Откройте `BreakingBonds.sln` в Visual Studio
4. ✅ Выберите конфигурацию Debug x64
5. ✅ Соберите проект (Ctrl+Shift+B)
6. ✅ Скопируйте DLL из `sfml\bin` в `bin\x64\Debug\`
7. ✅ Запустите игру (F5)

---

**Приятной игры и удачного изучения химии! 🧪**

*"Наука, вот в чем суть!"* — Уолтер Уайт
